# 技术债与待修问题清单

> 来源：对 `a5840ce..2f3c3a2` 这批改动做的三轮代码审查（词汇本后端 / 成长引擎与既有改动 / 前端）+ 自查。
> 已修复项见 commit `c2713b2` `477dba6` `430a521` `2f3c3a2`（Critical 1、High 6、Medium 11）。**本文件只列未修复项。**
>
> 工作量标记：**S** ≈ 半天内 ｜ **M** ≈ 1–2 天 ｜ **L** ≈ 3 天以上／需设计

## 进度

| 批次 | 项 | 状态 |
|---|---|---|
| 第一批（正确性） | #1 并发读改写事务边界 | ✅ 已修复（`begin_immediate`；12 线程并发实测不再丢更新） |
| 第一批（正确性） | #3 重新收藏丢学习历史 | ✅ 已修复（从 `user_unit_memory` 回填 SM-2，mastery 按迭代次数重建） |
| 第一批（正确性） | #4 已掌握阈值 | ✅ 已修复（`AND` → `OR`，兼容 mastery 高但 interval=0 的历史行） |
| 第一批（正确性） | #2 SM-2 双份真源 | 🟡 部分缓解（重新收藏已回填；完整单真源重构待做） |
| 第二批（性能/阻塞） | #5 连接数与同步阻塞 IO | ✅ 事件循环阻塞已修（`vocabulary.py` 33 个端点 `async def` → `def`）；连接数已批量化（review 8→2、写作 46→4） |
| 第二批（性能/阻塞） | #16 埋点半写 | ✅ 已修复（埋点写入合并为单连接单事务 + 失败回滚；新增原子性测试） |
| 补测 | #34 #37 #38 部分 | ✅ 新增 `tests/test_data_integrity.py`（5 项） |
| 新增 | 其余路由同类阻塞（ability / dashboard / mistakes / chat / speaking / listening / reading / writing 共 51 处 `async def` 内含同步 IO） | ⏳ 待做（机械转换 + 逐个确认无「未 await 调用协程」） |

---

## P0 架构级（影响数据正确性，建议优先排期）

| # | 问题 | 位置 | 影响 | 建议 | 量 |
|---|---|---|---|---|---|
| 1 | **并发读-改-写无事务边界 → 丢更新**。默认 `isolation_level=""` 下 SELECT 是自动提交，只有写才隐式 BEGIN，两个并发请求可读到同一快照后互相覆盖。注释宣称"单连接内读-改-写已解决"，与实现不符 | `db.py`：`update_task_progress`、`complete_daily_task_item`、`append_daily_task_items`、`update_user_skill_state`；`learning_event_service.py`：`_record_ability_sample`、`_upsert_learning_unit` | 并发完成任务/上报进度时静默丢数据；当前只是把"跨连接读"改成了"同连接读"，原子性并未建立 | 读改写前 `BEGIN IMMEDIATE`（或 `isolation_level=None` + 显式事务），或改单条 `UPDATE ... json_set(...)` / 乐观版本号重试 | M |
| 2 | **SM-2 状态双份真源**：`vocabulary.sm2_*` / `mistakes.sm2_*` 与 `user_unit_memory.sm2_*` 各存一份，跨连接读改写且无共同事务 | `db.py` `review_vocabulary`/`review_mistake`；`learning_event_service.py` `_calculate_unit_sm2`（读）与 `_upsert_user_unit_memory`（写） | 同一逻辑状态两处维护，任何只更新一处的路径都会分叉；并发复习同一 unit 双双读到 reps=N 并都写 N+1 | 确定唯一权威存储（建议 `user_unit_memory`），另一处只做投影；计算+写入放进同一事务 | L |
| 3 | **硬删除词汇 = 不可逆销毁学习历史**，且与成长引擎不一致。删行后 `learning_units`/`user_unit_memory` 仍保留；重新收藏后第一次 review 传入归零 SM2，而 `_upsert_user_unit_memory` 的 `DO UPDATE SET sm2_* = excluded.sm2_*` 会**无条件覆盖**，冲掉引擎积累的间隔/易度 | `db.py` `delete_vocabulary` / `delete_vocabulary_bulk`；`learning_event_service.py` `_upsert_user_unit_memory` | 用户误删即永久丢进度；重新添加后水平被重置 | 改软删除（`deleted_at`）并在列表/统计过滤；或重新收藏时从 `user_unit_memory` 回填 SM2 | M |
| 4 | **「已掌握」阈值实践上不可达 + 历史数据不兼容**。`mastery >= 0.9 AND interval >= 60`：SM-2 推演只有连续 5 次 q=5 才同时满足，而 q=5 只来自 `familiar`，`familiar` 默认**不入本** → 新词走不到。历史行迁移后 `sm2_interval_days = 0`，原本高掌握度的词全部回退为"学习中" | `db.py` `_mastered_thresholds` / `get_vocabulary_mastery_counts` / `get_vocabulary_page` | 「已掌握归档」形同虚设；（浮点漂移与容差已修，阈值可达性未修） | 阈值改 `mastery >= 0.9 OR interval >= 60`；对 `sm2_repetitions = 0 AND mastery_level >= 0.9` 的历史行做一次性回填迁移；阈值外置可配 | M |
| 5 | **单次请求连接/事务数过高 + async 路由内同步阻塞 IO**。实测一次写作提交开 **46** 个 sqlite 连接、一次 review **8** 个；新接口全是 `async def` 却直接调 sqlite/psycopg，而部署是单进程 uvicorn | `backend/routers/vocabulary.py` 全部 `/book/*`、`/explain*`；`ability_service.record_practice_result`；`db.py` 各 DAO | 阻塞事件循环、并发能力差、P99 延迟高；写入跨多事务可半写 | 接口改 `def`（FastAPI 自动丢线程池）或对 DB/PG 用 `run_in_threadpool`；隐藏引擎写入合并为单连接单事务、批量提交 | L |

---

## P1 数据质量与算法

| # | 问题 | 位置 | 影响 | 建议 | 量 |
|---|---|---|---|---|---|
| 6 | **冷启动 mastery 偏低，把"刚练好的能力"判为薄弱**：首次插入 `mastery = outcome * 0.35`（上限 0.35），而 weak 阈值 `< 0.58`、`< 0.45 → high`，且 weak 只要求 `exposure_count > 0` | `db.py` `update_user_skill_state`；`growth_recommendation_service.py` weak 过滤；`learning_event_service.py` `_risk_level` | 实测一次 8.6/10 的写作后 12 个 skill 全在 0.28–0.30，推荐全部指向刚做好的模块 | 首样本用校准后的 outcome（或统一 EMA）；weak/risk 要求 `exposure_count >= 2`；重标阈值 | M |
| 7 | **口语埋点用编造分数驱动成长曲线**：`content_score = word_count/3`、`language_score` 只有 5.5/7.5/8.0 三档，作为 `accuracy/grammar/vocabulary` 落到 `ability:speaking_*` 并成为难度推荐样本 | `backend/routers/speaking.py` | 成长曲线与难度推荐失真（"说得长 = 语法好"）；每个 turn 触发完整写入，热点接口 | 把"参与度/流利度"与"掌握度"信号分开，掌握度只取真实评分；隐藏写入改后台任务或批量 | M |
| 8 | **启发式关键词命中率直接当作自评质量**：`evaluateRecallAgainstDefinition` 的文本包含率写入 `todaySelfRating` 并上传为 `self_rating`，决定 SM2 quality | `frontend/src/pages/Vocabulary.jsx` | 用户用词不同但意思正确 → 被判 `forgot`；罗列释义关键词 → 得 `familiar`，噪声大 | 启发式只作"系统建议"，在 reveal 步显式提示并让用户确认/改写 | S |
| 9 | **练习干扰项只做了随机、未按词性/难度分带**：现在每次不同了，但仍跨词性混采 | `ielts_vocabulary_bank_service.sample_word_definitions` | 4 选 1 仍可能靠"词性不匹配"排除 | 采样时按同 `part_of_speech` + 同难度带过滤 | S |
| 10 | **自由文本 topic / unit_key 无界增长**：`topic: str` 无 `max_length`，`_normalize_key` 只过滤字符不截断，写入 `skill_tags` / `user_state` / `learning_units.unit_key` | `backend/routers/writing.py`（topic 字段）；`ability_service.py` `_normalize_key`；`learning_event_service.py` `_upsert_learning_unit` | 单次请求即可写入超长 key 并新增若干行，可被用于存储放大 | topic 截断（如 64 字符）并尽量收敛到受控枚举 | S |

---

## P2 健壮性

| # | 问题 | 位置 | 影响 | 建议 | 量 |
|---|---|---|---|---|---|
| 11 | **Redis 调用无 per-call 隔离、失效客户端不重置**。已加懒重连 + 1s 超时 + bytes/str 兼容，但 `_get_redis` 不做健康探测，单次操作出错也不置 `None` | `backend/redis_client.py` | Redis 重启后旧 client 仍被返回，鉴权与打卡日历可能 500 | 每次调用包 try/except 并回退内存；连接类异常时 `_redis = None` 触发重连 | S |
| 12 | **成长引擎挂在 Dashboard 关键路径且无降级**：任一新查询异常让整个 overview 500；`recommendations` 结构被替换，与旧字段不兼容；每条推荐调一次难度推荐（最多 5 次事件扫描） | `backend/services/dashboard_service.py` | 未迁移库或 `SQLITE_BUSY` 直接打挂首页；前端按旧字段渲染会退化 | try/except + 回退旧构建器；`recommendations` 保持向后兼容字段；难度推荐批量/缓存 | M |
| 13 | **TTS 细节**：`_load_minio` 静默吞异常无日志；`_ensure_minio` 持 `self._lock` 做网络 I/O（与 `synthesize` 共用锁 → 全量串行）；`health()` 在懒加载前 `minio_enabled` 恒 False；缓存命中不返回 `duration` | `backend/services/tts_service.py` | 首次探测期间所有 TTS 请求串行；监控数据误导；缓存命中响应缺字段 | 探测移出锁或改后台 + 超时；health 反映真实状态；缓存分支补 duration | S |
| 14 | **`sm2_*` 列只在 `init_db()` 里 `ALTER TABLE` 添加，不在任何迁移文件** | `db.py`；`migrations/` | 应用启动路径没问题，但"只跑 `migrations/*.sql`"的部署/迁移工具会得到缺列的库，随后 review 直接报错 | 补 `019_add_sm2_columns.sql`（或正式迁移机制记录） | S |
| 15 | **死表 `daily_learning_recommendations`**：迁移 018 建了但全仓无人读写 | `migrations/018_learning_growth_engine.sql` | schema 噪声 | 删除或落地使用 | S |
| 16 | **埋点半写**：`record_hidden_learning_event` 单次调用跨 4–6 个独立事务（`learning_units` 先于事件写入） | `learning_event_service.py` | 中途异常留下部分状态 | 单连接单事务批量提交 | M |

---

## P3 契约与一致性

| # | 问题 | 位置 | 影响 | 建议 | 量 |
|---|---|---|---|---|---|
| 17 | **`collect: Optional[bool]` 三态只实现一半**：只在"词不在本"时消费；词**已在**本中时 `collect=False` 被静默忽略（不移出），与模型注释"显式覆盖"矛盾 | `backend/routers/vocabulary.py` `grade_book_word` | 前端若依赖该语义会误用 | 要么在 `collect is False and target is not None` 时执行移出，要么把注释改成"仅对未入本词生效" | S |
| 18 | **分组 key 与过滤口径不一致**：`book_summary` 用原始 `source_module`，`book_list` 用 `COALESCE(NULLIF(TRIM(...)))`；`_BOOK_SOURCE_LABELS` 缺 `scenario_pack` | `vocabulary.py` / `db.py` | 空白 source 会产生一个点进去计数对不上的分组；部分来源显示原始 key | 分组也用 `COALESCE(NULLIF(TRIM(...)))`；标签表补全并加兜底 | S |
| 19 | **`status=active` 的 NULL 三值逻辑**：`NOT (mastery >= ? AND interval >= ?)` 在 `mastery_level IS NULL` 时整行为 NULL → 被排除；而计数函数把 NULL 计为 active | `db.py` `get_vocabulary_page` vs `get_vocabulary_mastery_counts` | 计数与列表可能不一致（当前无 NULL 数据，属潜在） | SQL 加 `OR ... IS NULL`，或给列加 `NOT NULL DEFAULT 0.0` | S |
| 20 | **`review_interval_seconds` 与 SM-2 interval 口径不一致**：自定义间隔覆盖了 `next_review_date`，但 `sm2["interval_days"]`/`sm2_interval_days`/label 仍是 SM-2 值 | `db.py` `review_vocabulary` | 与"已掌握（≥60 天）"判定自相矛盾，且写进 `user_unit_memory` | 覆盖时同步 `interval_days = (next_review_date - now)/86400` 并重算 label | S |
| 21 | **`review_vocabulary` / `review_mistake` / `delete_vocabulary` 内部不校验归属**，完全依赖调用方 | `db.py` | 当前 4 个调用点都有校验、不可利用；但属"少写一行就越权"的设计 | 把 `user_id` 提为必填参数并在 SQL 里 `WHERE id = ? AND user_id = ?` | S |
| 22 | **`session_id` 是死字段**：`BookSessionResponse.session_id` 随机生成、不落库、grade 不校验，前端也未使用 | `vocabulary.py` / `VocabularyStudy.jsx` | 无法校验"评分的词确实来自一次合法会话"；字段误导 | 落库并在 grade 校验归属，或直接删除该字段 | S |
| 23 | **阈值常量三处重复且硬编码**：`_mastered_thresholds` 被计数与分页各取用、Python 侧又算一次 | `db.py` | 可维护性 | 收敛到一处并外置可配 | S |

---

## P4 前端体验与可访问性

| # | 问题 | 位置 | 影响 | 建议 | 量 |
|---|---|---|---|---|---|
| 24 | **划词浮层定位不夹取视口边界**，且基于一次性 rect，容器滚动后不跟随 | `WordSelectionPopover.jsx` | 屏幕边缘选词时浮层溢出视口；滚动后位置错位 | 横向 clamp + `scroll`/`resize` 重定位 | S |
| 25 | **问老师面板自动滚动会顶掉释义**：`[thread, asking]` 变化都滚到底（一次提问执行两次） | `WordExplainPanel.jsx` | 用户刚看例句就被跳走 | 只对新消息追加滚动，或用 `scrollIntoView` 定位最后一条气泡 | S |
| 26 | **批量移出后列表塌回第一页**（已保留 offset 逻辑但为正确性仍重载第 0 页） | `VocabularyBook.jsx` | 翻了几页后移除会丢位置 | `loadPage` 支持 replace 模式后按当前 offset 重载 | S |
| 27 | **`practiceUsed` 是整批预算**，跨词不重置：默认 10 词批次最多 3 次深度练习（`forgot` 仅 2 次），第 4 个"不认识"直接落库 | `VocabularyStudy.jsx` | 用户体验像"练习突然消失" | UI 明示剩余练习额度，或改为按词计算 | S |
| 28 | **进度条语义易误读**：feedback 阶段显示 `index+1`（"正在做第 i 个"而非"已完成 i 个"） | `VocabularyStudy.jsx` | 轻微困惑 | 注释说明或按阶段调整 | S |
| 29 | **a11y 细节**：卡片 `role="button"` 无 `:focus-visible` 样式；`role="dialog"` 无 `aria-modal`/焦点陷阱/关闭后焦点回归；`window.confirm` 阻塞且不可样式化 | `VocabularyStudy.jsx` / `WordExplainPanel.jsx` / `VocabularyBook.jsx` | 键盘与读屏体验 | 补焦点样式与焦点管理；把 confirm 换成站内确认弹层 | S |
| 30 | **`API_BASE` + `toAudioSrc` 三处重复**（`VocabularyStudy.jsx`、`Vocabulary.jsx`、`utils/api.js`） | 同上 | `VITE_API_URL` 处理易再次分叉 | 抽到公共 util | S |
| 31 | **死状态与死 CSS**：`todayAttemptFeedback`、`quizVersion`、无 setter 的 state 降级为渲染期重建对象；`App.css` 残留 `.vocab-learning-hero*`、`.vocab-step-chip*`、`.ts-mode-*`、`.ts-guided-targets`、`.ts-inline-link`、`.ts-iteration-list` 等 20+ 条已无 JSX 消费 | `Vocabulary.jsx` / `Reading.jsx` / `App.css` / `TranslationSearch.jsx` | 维护成本 | 清理 | S |
| 32 | **Home 的推荐/任务卡片在 `route` 缺失时仍渲染**，点击无反应（`goToRoute` 静默 return） | `frontend/src/pages/Home.jsx` | 死按钮 | `route && <button>` | S |
| 33 | **划词收录的残留缺口**：非当前词且词库无该词条时，加入词汇本仍会得到空释义 | `VocabularyStudy.jsx` / `vocabulary.py` `_book_collect_data` | 词汇本出现"暂无释义"条目 | 后端在无 `bank_word_id` 且 definition 为空时按词查词库补齐 | S |

---

## P5 测试盲区

现有 `tests/test_learning_growth_engine.py`（7 项）覆盖了 SM-2 前两次间隔/EF 下限、词与错题复习落隐藏状态、quality 覆盖 delta、跨模块练习、智能任务完成与日历联动。**未覆盖**：

| # | 缺失项 | 能拦住的问题 |
|---|---|---|
| 34 | `save_mistake` / `save_vocabulary` 重复写入必须保留 SM-2/掌握度 | 本次核心不变量 |
| 35 | 个人词库带 `examples` 走 `format_core_words_for_prompt` | 已修的 Critical 漏网点 |
| 36 | 埋点失败不影响主流程、不半写 | H3 |
| 37 | `save_vocabulary` 并发写入不抛 IntegrityError | H2 |
| 38 | Dashboard 不重复计数（埋点事件被排除） | 指标翻倍 |
| 39 | SM-2 失败路径细节：q<3 的 0.25/1.0 间隔、EF 不下降、1.3 下限、`normalize_quality` 越界/非数值 | — |
| 40 | `_risky_abilities` 分级/排序/`include_normal`；`_due_memory_items` 的 NULL/0/等于 now 边界 | — |
| 41 | 旧库补列升级（缺 `sm2_*` 列时 `init_db` 能补齐） | 部署路径 |
| 42 | `review_interval_seconds`、`delete_vocabulary_bulk`、`get_vocabulary_page` 的 status/source 过滤、TTS/Redis 降级分支 | — |
| 43 | 测试卫生：`_init_tmp_db` 改全局 `DB_PATH` 不还原；Redis 用例写真实实例不清理 | 顺序耦合 |

---

## P6 运维与清理

| # | 问题 | 位置 | 建议 | 量 |
|---|---|---|---|---|
| 44 | `learning_events` 无去重/TTL：每跳过一个词写一行，长期无界增长 | `vocabulary.py` `_record_book_seen` | 按 (user, word, day) 去重 + 保留策略 | S |
| 45 | 限流桶清理不彻底（>5000 且仅清已过期）、且为进程内限流（多 worker 额度翻倍） | `vocabulary.py` `_allow_rate_request` | 概率/定时触发 + LRU 上限，或换 Redis 计数 | S |
| 46 | `_add_column_if_missing` 的 `ALTER TABLE {table}` 为 f-string | `db.py` | 改显式白名单映射（当前入参均为常量，不可利用） | S |
| 47 | `ensure_skill_tag` 冲突时 `metadata = excluded.metadata` 覆盖既有 metadata | `db.py` | 改为合并 | S |
| 48 | `get_growth_insights` 为计数加载最多 200 条到期行 | `learning_event_service.py` | 改 `COUNT(*)` | S |
| 49 | `_completed_today_task_ids` 只读 `status='active'` 计划，旧计划历史被忽略；`dt.date` 依赖"本地零点整数"隐式约定 | `learning_event_service.py` / `plan.py` | 抽公共 helper 并明确语义 | S |
| 50 | `reading.py` 用 `mode` 覆盖同名局部变量（仅用于 f-string，无副作用）；`question_type` 放进 result 后被丢弃 | `backend/routers/reading.py` | 改名 / 清理无用字段 | S |

---

## 建议排期

1. **第一批（正确性）**：#1 事务边界、#2 SM-2 单真源、#3 软删除、#4 已掌握阈值 + 历史回填
2. **第二批（性能/阻塞）**：#5 连接数与线程池、#16 埋点单事务
3. **第三批（算法质量）**：#6 冷启动、#7 口语埋点、#8 自评质量、#9 干扰项分带
4. **第四批（健壮性/契约）**：#11–#15、#17–#23
5. **第五批（前端与清理）**：#24–#33、#44–#50
6. **穿插补测**：#34–#43（其中 #34–#38 应随第一批一起补）
